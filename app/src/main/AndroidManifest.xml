<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.s.icpa">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ICPAMemeberApplication"></activity>
        <activity android:name=".ClaimfromRWC" />
        <activity android:name=".ClaimFormPMU" />
        <activity android:name=".FatigueReportForm" />
        <activity android:name=".Complaint_form" />
        <activity android:name=".MemberRegistraion" />
        <activity android:name=".Notification" />
        <activity android:name=".Contact" />
        <activity android:name=".Edit_Profile" />
        <activity android:name=".Profile" />
        <activity android:name=".TravelForm" />
        <activity android:name=".Change_email" />
        <activity android:name=".Change_Password" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>